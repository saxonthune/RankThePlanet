{
  "name": "rtp-statechart",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Generates inline-literal createMachine TypeScript files from carta statechart sidecars so the Stately VS Code extension can visualize them.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "xstate": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "chokidar": "^4.0.0"
  }
}
