{
  "surface": "Attributions",
  "regions": [
    {
      "id": "topBar",
      "purpose": "Identifies the surface and holds the affordance back to About."
    },
    {
      "id": "list",
      "purpose": "One row per library in the build's resolved dependency graph. Each row identifies the library and expands in place to reveal its license text — expansion is a display toggle, not a transition."
    }
  ],
  "affordances": [
    {
      "id": "back",
      "label": "Back",
      "region": "topBar",
      "event": "BACK",
      "target": "About"
    }
  ],
  "lists": [
    {
      "id": "libraries",
      "region": "list",
      "purpose": "The derived list of open-source dependencies — generated by the aboutlibraries Gradle plugin from the resolved POMs at build time, not hand-curated."
    }
  ]
}
